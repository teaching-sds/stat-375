name: Trigger site rebuild hourly

on:
  schedule:
    - cron: "32 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: nightly-deploy
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/61d684f359add353c02e4b16

